# 留白（LiuBai）V2 · 产品需求文档

- 状态：v1.0（全部章节完成）
- 日期：2026-09-12
- 范围：V2 全部 18 屏（桌面 s01–s14、移动端 s15–s18）的模块级需求、状态机、账本算术与验收边界
- 依据：design/UIUX_交互规范.md（§01–§08）、design/文件清单.json、design/01_桌面大图/*.png、design/02_移动端大图/*.png、src/styles/tokens.css

## 1. 产品概述与设计对象

留白的首要产品对象不是任务数量，而是**用户希望保住的一段时间**。任务、来源、外部承诺、AI 产物与授权全部围绕这段时间组织：先决定“哪段时间要保住”，再讨论由谁、用什么方法完成其余事项。

**设计对象与用户**：面向需要同时管理工作责任与个人时间的个人用户。产品必须服务两种时刻——安排工作时的判断时刻，以及处于保护时段内的安静时刻；两者的界面密度与语气完全不同。

**视觉方向**：白瓷、冰白玻璃、冷银结构与极少量状态色；开放椭环表达“可呼吸的边界”，不用能量球暗示 AI 全知。首页与留白时刻允许空间感；文档、权限与失败页面优先保证文字与操作可读性。不使用星球、风景或大面积光效铺满功能页面。

**哲学，落实为规则**：
- 不安排也是正常结果；
- 请求不自动成为责任；
- 延期不自动算省时；
- 草稿不自动等于完成；
- 用户可以拒绝、修正、暂停和退出。

任何功能设计若与上述五条冲突，以规则为准。

## 2. 设计规范与令牌

**排版基准**：桌面 1920×1200，内容区从 x=151 开始；主标题 42，正文 17—20，辅助说明 13—16，最终大图按 2× 输出。移动端 480×1040，主标题 31—35，正文 14—18，最终大图按 3× 输出。

**字体分工**（叙事与操作在字形上分开，不允许全部文字变成装饰）：中文叙事标题 Noto Serif CJK SC；中文功能文案 Noto Sans CJK SC；拉丁界面标签优先 Inter；时间数字采用衬线数字排版。

**色彩令牌**（已实现于 `src/styles/tokens.css`）：

| 令牌 | 值 | 用途 |
|---|---|---|
| `--bg` | #F3F5F6 | 页面背景 |
| `--text` | #232C32 | 主要文字 |
| `--text-2` | #65727B | 正文次级文字 |
| `--divider` | #DDE4E7 | 分隔线 |
| `--info` | #426D92 | 信息蓝（估计、进行中、提示） |
| `--ok` | #4D7771 | 确认绿（已同步、已授权、已保住） |
| `--warm` | #956C43 | 待确认暖色（未知、缺口、负担） |
| `--surface` / `--surface-2` | #FFFFFF / #FAFCFC | 面板层次 |
| `--graphite` | #2F3B42 | 主动作深石墨色 |
| `--focus` | #7A9FBA | 键盘焦点外框 |

层次由浅背景、细描边与阴影共同建立（`--shadow-panel`、`--shadow-btn`），不只靠透明度区分状态。

**形状与控件**：常规面板圆角 24—34（令牌 `--radius-sm/md/lg`＝24/28/34）；按钮高度桌面 49—60（`--btn-h`＝54）、移动端 47—56（`--btn-h-m`＝50）。主动作使用深石墨色，次动作用白色描边。**每个主要决策区域最多突出一个主动作**，避免满屏发光按钮争夺注意力。

**状态色语义**：颜色永远携带文字；图标与颜色不作为唯一状态表达（详见 §8、§6）。

## 3. 全局信息架构

**桌面 Shell**（`src/shell/Shell.tsx`）：左侧图标导航栏六项——此刻 / 时间 / 收件 / 协同 / 回顾 / 边界，底部为搜索（⌘K，唤起 s14 快捷输入）与用户头像；顶部品牌行“留白 / PERSONAL TIME”＋右侧“概念设计 · 演示状态”徽标与日期；页脚固定两条诚实声明（覆盖范围声明、演示状态声明）。桌面路由挂载在 Shell 内。

**桌面导航 → 屏幕映射**：

| 导航项 | 主屏 | 流程可达屏 |
|---|---|---|
| 此刻 | s01 `/` | s04 → s05（决定流）、s14（⌘K）、s08（进入保护时段） |
| 时间 | s02 `/ledger` | s04 → s05、s13（同步异常）、s08 |
| 收件 | s03 `/inbox` | s01（接受后形成责任） |
| 协同 | s06 `/workspace`、s07 `/session` | s09（注意力队列）、s10（规则差异） |
| 回顾 | s12 `/review` | s11（上下文修正入口） |
| 边界 | s10 `/boundaries` | s11 `/context`、s13、s09（打扰规则） |

**移动端裸路由**：s15–s18 挂载在 `/m/now`、`/m/plan`、`/m/auth`、`/m/blank`，不使用桌面 Shell；单列版式，各自携带状态栏区、品牌行与底部标签栏（此刻 / 时间 / 协同 / 我的），安静界面（s18）隐藏标签栏仅保留退出按钮。

**路由约定**：每屏一个路由、一个 `data-page` 标识；授权预览（s05/s17）只能由方案页（s04/s16）进入；安静界面（s08/s18）为全屏态，进入后隐藏常规导航，退出后回到来源屏。

### 3.1 18 屏路由总表

| 编号 | 屏幕 | 路由 | 设计大图 | 核心信息 | 用户下一步 | 必须保留的边界 |
|---|---|---|---|---|---|---|
| s01 | 此刻 · 时间主权 | `/` | 01_桌面大图/01_此刻_时间主权.png | 要保护的时间与当前容量缺口 | 查看可行做法 | 没有执行变更不能写成已优化 |
| s02 | 时间账本 · 责任与边界 | `/ledger` | 01_桌面大图/02_时间账本_责任与边界.png | 安排轨与保护轨、来源覆盖 | 查看方案或处理冲突 | 未同步不是空闲 |
| s03 | 收件箱 · 请求不等于承诺 | `/inbox` | 01_桌面大图/03_收件箱_请求不等于承诺.png | 原话、提出者、接受者、期限 | 接受、保留想法或排除 | 请求不能自动升级为承诺 |
| s04 | 自适应方案 · 改变方法与分工 | `/plan` | 01_桌面大图/04_自适应方案_改变方法与分工.png | 减负与延期的不同后果 | 选择一条路径 | 估计、净减少与未来负担分开 |
| s05 | 变更预览 · 有限授权 | `/preview` | 01_桌面大图/05_变更预览_有限授权.png | 对象级前后差异与授权范围 | 批准本次或返回修改 | 参数或计划变化使旧批准失效 |
| s06 | 协同工作台 · 有来源的草稿 | `/workspace` | 01_桌面大图/06_协同工作台_有来源的草稿.png | 可检查草稿、来源与两个判断 | 核对、补充、确认已检查内容 | 未核对成本不能伪装为结论 |
| s07 | 工作现场 · 恢复下一步 | `/session` | 01_桌面大图/07_工作现场_恢复下一步.png | 已确认取舍与尚未决定的问题 | 继续判断或换成口述 | 恢复真实检查点，不发明进度 |
| s08 | 留白时刻 · 不安排也成立 | `/blank` | 01_桌面大图/08_留白时刻_不安排也成立.png | 这段时间由本人决定 | 不安排、继续音乐或探索 | 没选择不再催促 |
| s09 | 注意力队列 · 同一份预算 | `/attention` | 01_桌面大图/09_注意力队列_同一份预算.png | 期限、后果与合并规则 | 查看重要差异或修改提醒规则 | AI 内部请求使用同一预算 |
| s10 | 我的边界 · 权限与暂停 | `/boundaries` | 01_桌面大图/10_我的边界_权限与暂停.png | 读取、准备、写入、外部动作的权限 | 修改规则或暂停 | 模型不能自行扩大权限 |
| s11 | 私人上下文 · 来源与推测 | `/context` | 01_桌面大图/11_私人上下文_来源与推测.png | 本人意图、AI 假设、数据来源 | 修正、撤销或导出 | 推测不能升级为事实 |
| s12 | 时间回顾 · 收益不做假账 | `/review` | 01_桌面大图/12_时间回顾_收益不做假账.png | 保住的时段、延期负担、监督成本 | 低频反馈或跳过 | 不能伪造净节省与人生评分 |
| s13 | 同步异常 · 未知与接管 | `/sync` | 01_桌面大图/13_同步异常_未知与接管.png | 本地规则与外部保护不同状态 | 重试、原工具接管、暂用本地计划 | 外部保护未核验就保持未知 |
| s14 | 快捷输入 · 意图与约束 | `/capture` | 01_桌面大图/14_快捷输入_意图与约束.png | 自然语言中的意图与约束 | 检查冲突或只保存 | 输入一句话不等于泛化授权 |
| s15 | 移动端 · 此刻 | `/m/now` | 02_移动端大图/15_移动端_此刻.png | 一段时间与一个必要决定 | 查看两条路径 | 不显示任务完成量作为中心 |
| s16 | 移动端 · 自适应方案 | `/m/plan` | 02_移动端大图/16_移动端_自适应方案.png | 新投入估计与延期代价 | 进入授权预览 | 固定底部动作不能跳过批准 |
| s17 | 移动端 · 一次性授权 | `/m/auth` | 02_移动端大图/17_移动端_一次性授权.png | 允许的三项动作与排除项 | 批准本次或返回 | 不包含发送、改会与付款 |
| s18 | 移动端 · 留白时刻 | `/m/blank` | 02_移动端大图/18_移动端_留白时刻.png | 简洁的个人时间状态 | 什么也不安排 | 无绩效、打卡与倒计时压力 |

## 4. 模块分解（s01–s18）

<!-- 每屏：目的 / 布局区域 / 数据模型 / 交互 / 状态 / 验收标准 / 必须保留的边界；与设计大图 1:1 对应 -->

### s01 此刻 · 时间主权

**设计大图**：`design/01_桌面大图/01_此刻_时间主权.png`（对应图号 01）。

**目的**：首页以“要保护的一段时间”为主对象（今晚 19:00—20:00），同时呈现当前容量缺口（10 分钟）与一个需要用户决定的单一问题，把用户下一步引向“查看可行做法”。

**布局区域**：
- 左侧图标导航栏：此刻 / 时间 / 收件 / 协同 / 回顾 / 边界，底部搜索（⌘K）与头像。
- 顶部品牌行：留白 / PERSONAL TIME；右侧“概念设计 · 演示状态”徽标与日期（09.12 · 星期六）。
- 主区“时间意图”卡：衬线大字 19:00—20:00；副文案“不自动填入工作。暂时没有用途，也成立。”；暖色警告 pill“当前计划超出容量 10 分钟”；主动作“看看可行的做法 →”与诚实副文案“还没有修改任何安排”；开放椭环视觉。
- 右列决策卡“01 此刻，只需一个决定”：问题“怎样把这十分钟找回来？”；并列 120（可用时间/分钟）与 130（当前投入估计/分钟）；两个候选方向：“准备报告草稿，减少从零开始（预计减少 20 分钟，仍需你检查确认）”与“或者，把行政事项移到明天”。
- 下区“02 没有被悄悄改变的责任”三张责任卡：已确认会议 30 分钟（固定约定 · 不自动移动）、报告交付 60 分钟（19:00 截止 · 标准不变）、行政事项 40 分钟（个人计划 · 可以重新决定）。
- 页脚：覆盖范围声明（工作日历、任务清单、指定文件；其他生活安排仍需确认）与“界面为独立演示状态；不代表已连接、已执行或真实收益”。

**数据模型**：
- `protectedBlock`：`{ start: "19:00", end: "20:00", purpose: null, lockedBy: "localRule" }`（用途允许为空）。
- `capacity`：`{ availableMinutes: 120, plannedMinutes: 130, gapMinutes: 10 }`。
- `commitments[]`：`{ title, minutes, kind: "固定约定" | "截止任务" | "个人计划", mobility: "不可自动移动" | "可重新决定" }`。
- `decision`：`{ question, options: [{ label, expectedReductionMinutes?, note }] }`；状态字段 `modifiedAnything: false` 支撑“还没有修改任何安排”。

**交互**：主动作与右列候选方向均进入 s04 方案页；责任卡只读展示移动性，不提供“悄悄移动”入口；搜索唤起全局快捷输入（s14）。

**状态**：
- 空态：未设置保护时段时给出发起保护的入口；无容量缺口时显示“容量充足”，不得编造缺口。
- 失败/过期：来源未同步时账本数据标注过期并保留上次数据，不回退为空列表（§05）。
- 边界：缺口为 0 或负值时如实显示；未连接真实来源时演示数据必须带“演示状态”徽标。

**验收标准**：
1. 首屏视觉主对象是保护时段 19:00—20:00，而不是任务完成量。
2. 容量缺口 10 分钟以文字 + 暖色 pill 双重呈现。
3. 页面不出现“已优化 / 已节省”字样；主动作旁可见“还没有修改任何安排”。
4. 三张责任卡各自显示移动性属性，且没有批量移动入口。

**必须保留的边界**：没有执行变更不能写成已优化；不自动填入工作；暂时没有用途也成立。

### s02 时间账本 · 责任与边界

**设计大图**：`design/01_桌面大图/02_时间账本_责任与边界.png`（对应图号 02）。

**目的**：以“安排与责任”和“你选择的边界”两条轨道呈现同一天，使 19:00 的容量冲突直接可见，并保证留白不能被自动侵占。

**布局区域**：
- 标题区：“时间有去向，也有边界。”；副文案“固定约定、可变计划与留白分开记录；空白不自动意味着可占用。”
- 日历卡：日期 9 月 12 日，今日/本周切换，时区 UTC+8；时间轴 17:00—20:30 分双列：左轨“安排与责任”依次为已确认会议（30 分钟 · 固定约定，带锁形图标）、报告交付（预计投入 60 分钟，pill“19:00 前提交”）、行政事项（40 分钟 · 暂定计划），其越界部分以斜线阴影 + “超出边界 10 分钟”警告呈现；右轨“你选择的边界”含胶囊“未提前安排的时间 ≠ 必须工作”与“受保护的留白 19:00—20:00”卡（“没有用途，也不接受自动填充。”）。
- 轴底说明：“需要改变完成路径，或由你重新决定可变事项。”
- 右列“01 今天的容量”：120 分钟可用 / 130 分钟当前预计投入 / 10 分钟容量缺口（暖色）。
- 右列“02 每件事都保留来源”：工作日历 17:00 已同步；任务清单 17:00 已同步；其他安排 未覆盖（暖色）。
- 右列收束卡：“移动，不等于节省。如果把工作移到明天，今天会更宽裕，但未来责任仍在。”＋主动作“查看可行方案 →”。

**数据模型**：
- `dayTimeline`：`{ date, timezone, lanes: { responsibility: TimelineEvent[], boundary: ProtectedBlock[] } }`。
- `TimelineEvent`：`{ title, start, end, minutes, kind: "固定约定" | "暂定计划" | "截止任务", sourceRef, overflowMinutes? }`。
- `sourceRegistry[]`：`{ name, lastSyncAt, coverage: "已同步" | "过期" | "同步失败" | "未连接" | "未覆盖" }`。
- `conflict`：`{ range: "19:00—20:00", gapMinutes: 10, overflowEventId }`。

**交互**：今日/本周切换；事件点开查看来源与状态；越界/冲突区域跳转 s04；暂定计划可由用户重新决定；留白卡只读，不响应拖拽填充。

**状态**：
- 空态：轨道为空时显示来源说明与手动记录入口，不伪造现成任务（§05）。
- 来源过期/失败：事件保留并标注最后成功同步时间与范围，不当作空闲。
- 边界：计划侵入保护段时以斜线阴影 + 警告呈现；“其他安排 未覆盖”必须显示为未知，不得折算成可用容量。

**验收标准**：
1. 双轨在视觉上可区分，且各自有列标题。
2. 19:00 冲突以越界阴影 + “超出边界 10 分钟”文字同时呈现。
3. “其他安排”行显示“未覆盖”，页面任何位置不把它呈现为空闲。
4. 留白卡不可被拖拽或自动填充。

**必须保留的边界**：未同步不是空闲；空白不自动意味着可占用；移动，不等于节省。

### s03 收件箱 · 请求不等于承诺

**设计大图**：`design/01_桌面大图/03_收件箱_请求不等于承诺.png`（对应图号 03）。

**目的**：把外部请求从“原话 + 来源 + 未知字段”完整呈现到用户确认为止，明确区分请求、意图与已接受责任。

**布局区域**：
- 标题区：“先理解，再成为责任。”；副文案“把外界的请求放进收件箱，不直接放进你的人生。”
- 左列“待澄清（3）”列表：竞品分析 · 会议提及（来源：项目例会摘录，尚未接受）；下周碰一次方案（来源：邮件 · 陈同事，时间待确认）；找个晚上练琴（来源：自己 · 快捷记录，个人意向）；底部文案“允许一个想法一直只是想法。收集，不等于接受。”
- 中列详情卡：状态 pill“请求 · 未确认”；标题“三种方案的差异”；元信息“例会摘录 · 09:32 · 合成示例”；原话引用块“下周可以看看三种方案的差异。”；“影响判断的四件事”字段组：提出者＝陈同事（演示人物）、谁已接受＝尚未确认（暖色）、截止期限＝尚未约定（暖色）、预计投入＝尚未估计；说明“只有你的确认，才会把它变成你的责任。系统不会替你回答‘我来做’。”；三个动作：我接受这件事（主动作）/ 保留为想法 / 不是我的事项。
- 右列“01 提取不是承诺”三步：已提出（会议中出现的请求）→ 待确认（系统提取出的候选事项）→ 已接受（只有你能建立这份责任）。
- 右列收束卡：“AI 只补问必要的问题。你是否愿意接受这件事？确定之后，再讨论范围与时间；不要求先填写一整套项目表格。”＋ pill“不自动向任何人发送消息”。

**数据模型**：
- `request`：`{ id, verbatim, sourceRef: { type, excerptAt }, requester, status: "已提出" | "待确认" | "已接受" | "保留为想法" | "已排除", acceptedBy: null, deadline: null, effortEstimate: null }`——未知字段必须保持 null 并显示占位词。
- 列表项派生字段：`sourceLabel`、`statusLabel`。

**交互**：列表项切换详情；“我接受这件事”将状态置为已接受并进入责任范围/截止时间讨论；“保留为想法”“不是我的事项”分别归档或排除；任何动作都不触发对外发送。

**状态**：
- 空态：无请求时显示来源渠道说明与手动记录入口。
- 未知态：谁已接受 / 截止期限 / 预计投入未知时显示“尚未确认 / 尚未约定 / 尚未估计”，系统不得代填。
- 失败：原话缺失时展示摘要但明确标注“原话缺失”，不得伪装成原话。

**验收标准**：
1. 原话引用块与来源元信息同屏可见。
2. 三个处置动作齐全，且接受动作不产生任何对外消息。
3. 四个判断字段全部呈现，未知值用暖色占位词而非默认值。
4. “提取不是承诺”三步进度中“已接受”未点亮。

**必须保留的边界**：请求不能自动升级为承诺；收集不等于接受；系统不代答“我来做”，不自动向任何人发送消息。

### s04 自适应方案 · 改变方法与分工

**设计大图**：`design/01_桌面大图/04_自适应方案_改变方法与分工.png`（对应图号 04）。

**目的**：并排比较“减负”与“延期”两条路径的不同后果，把预计减少与未来负担分开记账，让用户选择而非被安排。

**布局区域**：
- 标题区：“换一种做法，不挤掉自己。”；副文案“同一个结果，两条路径。改变的是做法，不是偷偷降低承诺。”
- 约束条：当前约束“19:00 前可用 120 分钟；当前预计投入 130 分钟。”；chips：会议不动 / 交付标准不变 / 19:00—20:00 留白 / 方案未执行（暖色）。
- 方案 A 卡（减少人的投入）：“先准备，再由你判断。AI 整理指定材料与比较草稿。你从检查和取舍开始，不再面对空白页。”；衬线数字 60 → 40 分钟（报告的预计人工投入 · 新估计包含检查与修改）；行项：预计减少人工投入＝20 分钟、19:00 前预计总投入＝110/120 分钟、仍需你完成＝内容检查与最终确认；主动作“选择这条路径 →”与副文案“下一步预览具体授权”。
- 方案 B 卡（把负担移到未来）：“今天放下，明天仍在。把行政事项移到明天。今天更宽裕，但这份责任不会消失。”；衬线数字 40 分钟（从今天转移到明天的工作量）；行项：预计净节省＝没有发生、19:00 前预计总投入＝90/120 分钟、明天新增负担＝40 分钟（暖色）；次动作“预览延期的影响 →”与副文案“不会算作节省时间”。
- 页脚：“估计不是事实。执行期间会更新人工投入，必要时重新检查可行性。”＋“保留原计划”。

**数据模型**：
- `plan`：`{ id, version, kind: "reduceEffort" | "defer", constraints: string[], estimate: { beforeMinutes, afterMinutes, expectedReductionMinutes, totalAfter, humanTasksRemaining }, futureDebt?: { minutes, dueDate }, status: "候选" | "选中" | "待批准" | "失效" }`。
- `expectedReductionMinutes` 一律为估计值，展示时必须带“预计”限定。

**交互**：选择方案 A → s05 变更预览；方案 B 的“预览延期的影响”→ 延期影响视图（仍不入账节省）；“保留原计划”→ 返回 s02；执行期间估计更新后需重新检查容量（§06）。

**状态**：
- 方案状态机入口：候选 → 选中；来源变化后置为失效并提示重新检查（§05）。
- 空态：无候选方案时显示来源与约束，不编造方案。
- 边界态：实际核对超过估计时，回到容量检查并允许撤回选择。

**验收标准**：
1. 方案 A 的“20 分钟”出现在“预计减少人工投入”行内，且注明新估计包含检查与修改。
2. 方案 B 的“预计净节省”显示“没有发生”，40 分钟显示为“明天新增负担”。
3. 全页不出现“节省了 40 分钟”类文案。
4. 约束条与“方案未执行”状态可见。

**必须保留的边界**：估计、净减少与未来负担分开；估计不是事实；方案未执行不算已安排。

### s05 变更预览 · 有限授权

**设计大图**：`design/01_桌面大图/05_变更预览_有限授权.png`（对应图号 05）。

**目的**：在批准前完整呈现对象级前后差异、一次性授权范围与排除项，并把批准绑定到具体计划版本。

**布局区域**：
- 标题区：“授权这一次，不是以后每一次。”；副文案“所有会改变的对象，都先出现在这里。你批准的是具体范围。”
- 左卡：pill“待批准 · 方案版本 03”；标题“准备报告草稿”；说明“只读取所选的两份示例材料，在留白中生成未发送的草稿。”；对象差异表（对象 / 原状态 / 批准后）：报告工作草稿：尚未创建 → 生成 · 待检查；报告投入估计：60 分钟 → 暂估 40 分钟；日历、消息与交付：没有变更 → 仍然没有变更。
- 左卡规则块“01 任何参数改变，都重新检查”：“材料范围、目标对象或计划版本发生变化时，旧批准不能继续使用。执行前仍会读取最新状态。”；排除 chips：外部约会 / 邮件发送 / 付款与购买——本次均不包含。
- 右卡“02 这次允许做什么”：三项勾选——读取指定材料（产品说明与访谈节选（示例））、创建一份本地草稿（输出保留来源，状态为待检查）、更新内部投入估计（标注为估计，可修正、可撤回）；说明“不包含发送、提交或移动会议。草稿创建可以撤回；对外发送无法假定‘一键撤销’，因此本次不授权。”；pill“有效范围：本次准备任务”；主动作“批准并准备草稿 →”；次动作“返回修改方案”。

**数据模型**：
- `changeSet`：`{ planVersion: 3, diffs: [{ object, before, after }], grants: ["读取指定材料", "创建一份本地草稿", "更新内部投入估计"], exclusions: ["外部约会", "邮件发送", "付款与购买"], scope: "本次准备任务" }`。
- `approval`：`{ id, planVersion, status: "待批准" | "有效" | "失效", boundPlanHash }`；参数或计划变化使旧批准失效。

**交互**：批准 → 按三项授权创建本地草稿（草稿状态：生成中 → 待检查）；“返回修改方案”→ s04，且不丢失候选方案（§05）；任何 diff 项变化后回到待批准并使旧 approval 失效。

**状态**：
- 操作状态机挂接：未开始 / 执行中 / 核验中 / 已核验 / 失败 / 未知；执行中说明当前动作，不虚构百分比进度。
- 失效态：版本或材料范围变化时明确显示“旧批准已失效，需重新批准”。
- 不可逆边界：若已发生不可逆外部动作，关闭界面不被解释为撤销；本页通过排除项从源头避免此类授权。

**验收标准**：
1. 差异表以三行“对象 / 原状态 / 批准后”完整呈现。
2. 三项授权与三项排除项同屏可见。
3. 批准徽标显示“方案版本 03”，版本绑定可追踪。
4. “有效范围：本次准备任务”可见，不存在常驻授权入口。

**必须保留的边界**：参数或计划变化使旧批准失效；不可逆外部动作本次不授权；撤销访问只控制后续访问。

### s06 协同工作台 · 有来源的草稿

**设计大图**：`design/01_桌面大图/06_协同工作台_有来源的草稿.png`（对应图号 06）。

**目的**：提供正文优先的文档工作台，把 AI 准备的草稿与用户的两个判断并置，缺失依据与未发送状态始终可见。

**布局区域**：
- 标题区：“让 AI 准备，让判断回到你。”；副文案“草稿不是完成，确认不是发送；每一步有自己的状态。”
- 左侧正文卡：pill“草稿 · 待检查”与“自动保存于 17:21”；标题“方案比较 · 工作草稿”；目标行“为下一轮产品方向讨论，保留可核查的取舍依据。”；来源 chips：产品说明（示例）、访谈节选（示例）；章节“01 两种路径，不同的负担”为三列比较表（比较项 / 完整工作台 / 个人协调层）：用户首先需要做什么＝迁入事项，维护新的系统 对 选择来源，提出一个意图；首版需要验证什么＝替代既有工具的理由 对 减少协调与监督负担；章节“02 仍然不能下结论的地方”：“成本数据尚未核对。不能仅凭功能描述，推断任何路径的留存、盈利或长期效果。”；卡底 pill“本文内容为设计演示，不是实际研究结果”＋撤销/导出图标。
- 右列“01 接下来，只需两个判断”：判断一“先确认产品切入点——是否以‘一个真实场景的协调减负’作为首版，而非替换全部任务工具？”，动作“同意这个取舍 / 我有不同判断”；判断二“成本核对还没有完成——缺少可靠成本材料。可以补充文件，或把它保留为待核对事项。”，动作“补充材料 +”；辅助行“预计人工检查 12—20 分钟”；主动作“确认已检查的内容 →”；下方说明“确认后仍不会自动发送或提交”。

**数据模型**：
- `draft`：`{ id, title, goal, status: "生成中" | "待检查" | "部分确认" | "已确认" | "已发送", sources: [{ label, kind }], sections[], openQuestions[] }`。
- `judgment`：`{ question, options: ["同意这个取舍", "我有不同判断"], answer: null }`。
- `audit`：`{ autoSavedAt, sentAt: null }`；确认动作只推进草稿状态，不改变 `sentAt`。

**交互**：两个判断分别作答；“补充材料”挂接文件后重算待核对事项；“确认已检查的内容”将已核对部分置为已确认（未核对部分保持待核对）；撤销/导出可达；确认永不触发发送或提交。

**状态**：
- 草稿状态机入口：生成中 → 待检查 → 部分确认 → 已确认；状态词即责任边界（§05）。
- 缺失依据：成本未核对章节常驻可见，不因确认而折叠消失。
- 失败：生成中断时保留上一份草稿并标注过期，不回退为空文档。

**验收标准**：
1. 两个判断块与各自动作可见，未作答状态明确。
2. “仍然不能下结论的地方”章节在确认前后均可见。
3. 主动作副文案“确认后仍不会自动发送或提交”存在。
4. 来源 chips 与“自动保存于”时间可见。

**必须保留的边界**：未核对成本不能伪装为结论；草稿不是完成，确认不是发送。

### s07 工作现场 · 恢复下一步

**设计大图**：`design/01_桌面大图/07_工作现场_恢复下一步.png`（对应图号 07）。

**目的**：被打断后把工作现场（已确认取舍、正在看的材料、未决问题）真实恢复，支持更换参与方式继续，而不是重复建立计划。

**布局区域**：
- 标题区：“从刚才那个判断继续。”；副文案“恢复的不只是任务名字，而是你上次离开的工作现场。”
- 左卡：pill“工作检查点 · 17:26”；主文案“你不必再讲一遍。”；“上次确认的取舍、正在看的材料、还没有决定的问题，都在这里。”；开放椭环视觉；收束句“先完成一个判断。不必一次重启整个项目。”
- 右列“01 上次停在这里”：主题“个人协调层 · 首版范围”；pill“已确认”＋“不要求迁移整套任务；只接一个真实来源。”
- 右列“02 现在需要你决定”：问题“草稿可以先准备，还是每次都询问？”；说明“当前边界允许整理指定材料，但不允许对外发送。你可以调整准备阶段的主动程度。”；候选规则卡“✦ 只在已选来源内，自动准备可撤回草稿——这是一个候选规则，还没有生效。”；动作“查看规则差异 →”（主动作）与“先口述我的想法”（带麦克风图标）。
- 右列“相关材料”：方案比较草稿、✓ 上次的两项确认；链接“查看完整记录”。

**数据模型**：
- `checkpoint`：`{ savedAt: "17:26", artifactVersion, confirmedDecisions: [{ label: "个人协调层 · 首版范围", note }], openQuestion: { text, options[] }, materials: [{ label, confirmed?: boolean }], nextStep }`。
- `ruleCandidate`：`{ text: "只在已选来源内，自动准备可撤回草稿", status: "候选" | "已生效" }`；候选状态不产生任何自动行为。

**交互**：重开时核对来源版本后恢复检查点；“查看规则差异”展开候选规则与现行规则的差别；“先口述我的想法”切换为口述输入；“查看完整记录”展开检查点历史；用户可继续、补材料或暂不处理（§04）。

**状态**：
- 版本核对：来源版本变化时提示重新检查，再恢复已确认部分（§04）。
- 空态：无检查点时显示“没有可恢复的检查点”，不生成占位进度。
- 边界态：模型推测只能以候选/未决呈现，不得占据“已确认”位置。

**验收标准**：
1. 检查点显示保存时间（17:26）与已确认 pill，未决问题单列。
2. 候选规则明确标注“还没有生效”。
3. 恢复流程不产生编造的进度或百分比。
4. 口述入口可达。

**必须保留的边界**：恢复真实检查点，不发明进度；模型自己的推测不能占据“已确认”位置。

### s08 留白时刻 · 不安排也成立

**设计大图**：`design/01_桌面大图/08_留白时刻_不安排也成立.png`（对应图号 08）。

**目的**：为保护时段提供全屏安静界面，把“不安排”作为一等选择，不设任何绩效压力。

**布局区域**：
- 顶栏：留白 / A SPACE OF YOUR OWN；右侧 pill“已接入渠道内受保护”与退出按钮（×）。
- 主区：pill“今晚 · 留给自己”；衬线大标题“这段时间，不必证明什么。”；时间“19:00—20:00”；“按你的边界保留。不自动补入下一件事。”；“你可以继续重要的事，也可以什么都不安排。”；动作：什么也不安排（深色主动作）、继续上次的音乐（次动作，带音符图标）；链接“更多可能性 →”；诚实说明“不会因为没有选择，而再次提醒你。”；右侧大面积开放椭环空间视觉。
- 页脚：“留白 · 安静界面”｜“保护不等于全渠道拦截；未接入的安排仍需你确认。”｜“08/14 · 设计演示”。

**数据模型**：
- `quietSession`：`{ block: { start, end }, choices: ["什么也不安排", "继续上次的音乐", "更多可能性"], rePromptAfterNoChoice: false, protectionScope: "已接入渠道内" }`。
- 本屏无任务列表、无计数器、无计时字段。

**交互**：三个选择均成立且无默认倒计时；选择“什么也不安排”后关闭会话且不再催促；“×”与页脚说明提供清晰退出；音乐恢复依赖既有授权。

**状态**：
- 安静态：动效不持续抢注意力，遵循系统“减少动态效果”（§07）。
- 边界态：保护只覆盖已接入渠道，页脚必须如实说明；未接入渠道的安排仍走确认流程。
- 失败：音乐源不可用时给出可读理由，不打断安静界面主体。

**验收标准**：
1. 页面无倒计时、无打卡、无完成量、无连续记录元素。
2. “什么也不安排”是主动作且选择后不再催促。
3. 退出入口（×）与“保护不等于全渠道拦截”说明可见。
4. 19:00—20:00 与“不自动补入下一件事”同屏。

**必须保留的边界**：没选择不再催促；不安排也成立；保护不等于全渠道拦截。

### s09 注意力队列 · 同一份预算

**设计大图**：`design/01_桌面大图/09_注意力队列_同一份预算.png`（对应图号 09）。

**目的**：用统一注意力预算约束外部请求与 AI 建议，按后果与期限排序，使 AI 内部请求不享有绕过规则的特权。

**布局区域**：
- 标题区：“少一些声音，重要的仍然可达。”；副文案“外部请求与 AI 建议，共用你设定的注意力预算。”
- 左卡顶部 pill“按后果与期限，不按发送者的‘紧急’”。
- 左卡“01 此刻需要你判断”：暖色警告卡“交付材料有更新，可能影响正在确认的报告”；依据行“有依据的期限：19:00 · 来源：你指定的工作材料”；后果行“不查看的可能后果：沿用旧材料中的成本说明。”；动作“查看差异 →”。
- 左卡“02 合并到下一次查看”三行：三个非紧急请求（两条会议候选时间 · 一条材料补充）→“18:30 合并重现”；一份明日准备摘要（AI 已默默准备，没有触发提醒）→“留在协同工作台”；可选的周末活动建议（与你最近的方向有关；没有真实期限）→“不主动打断”。
- 右列“03 你决定主动性的尺度”：衬线数字“1 次”＝今天剩余的主动提醒预算；“你设定：每天最多 2 次非紧急提醒。”；“真实责任风险单独呈现，不伪装成普通通知。”；粗体“预算不构成遗漏重要承诺的理由。”
- 右列收束卡：“AI 没有例外通道。新的建议可以先准备好，不必马上叫你来看。付费推荐也不能购买更高的打扰权限。”；动作“修改提醒规则”；页脚“覆盖：已接入请求；不是全系统通知接管。”

**数据模型**：
- `attentionItem`：`{ id, kind: "需判断" | "合并" | "静默准备" | "建议", source: "external" | "ai", deadlineEvidence?: { time, sourceRef }, consequence?, disposition: "立即呈现" | "合并重现 HH:mm" | "留在工作台" | "不主动打断" }`。
- `attentionBudget`：`{ dailyNonUrgentLimit: 2, usedToday, remainingToday: 1, appliesTo: ["external", "ai"] }`。

**交互**：“查看差异”进入 s06；“修改提醒规则”进入 s10 打扰规则；合并项显示重现时间；静默项不产生通知但可被发现。

**状态**：
- 期限核验：仅当期限有依据（来源 + 时间）时才进入“此刻需要你判断”，否则降级合并。
- 预算耗尽：剩余 0 时新非紧急项全部进入合并队列；真实责任风险仍单独呈现。
- 空态：队列为空时显示预算与规则入口，不制造待办。

**验收标准**：
1. 唯一判断卡有期限依据与后果说明，非紧急项全部合并或静默。
2. AI 摘要行明确“没有触发提醒”。
3. 剩余预算（1 次）与用户设定（每天最多 2 次）同屏。
4. “AI 没有例外通道”与付费限制文案可见。

**必须保留的边界**：AI 内部请求使用同一预算；不按发送者的“紧急”排序；付费不能购买更高的打扰权限；预算不构成遗漏重要承诺的理由。

### s10 我的边界 · 权限与暂停

**设计大图**：`design/01_桌面大图/10_我的边界_权限与暂停.png`（对应图号 10）。

**目的**：把读取、准备、写入与外部动作的权限分级呈现为可读规则，提供规则变更记录与一步暂停路径。

**布局区域**：
- 标题区：“智能可以学习，边界由你决定。”；副文案“规则不是提示词。每次读取、写入与打扰，都受它约束。”
- 左卡“我的规则”分组：行动权限（选中）、时间边界、打扰规则、暂停与退出；底部：规则版本 08、由你在 09.12 09:10 确认；引言“AI 可以提出修改建议，但不能自行让它生效。”；动作“查看规则变更记录 →”。
- 右卡“行动权限”（角标“本人的规则优先”）六行权限：
  1. 读取指定来源——只读工作日历、任务清单与手动选择的文件——已授权（绿）。
  2. 准备可撤回的草稿——限定材料范围；不代表内容已核验或交付已完成——可自动准备（蓝）。
  3. 改写内部计划——只在已授权时段内；不得移动外部约定——先预览。
  4. 移动外部会议——涉及他人的时间，必须按共同约定确认——每次确认（暖）。
  5. 发送消息或提交交付——批准对象与内容；发送不视为可完全撤回——每次确认（暖）。
  6. 付款、购买与新增承诺——当前未开放；不得从其他许可推导授权——未授权。
- 底部暂停条：“暂停全部自动化——原始事项与未完成责任仍然可见。”＋动作“立即暂停 ‖”。

**数据模型**：
- `ruleSet`：`{ version: 8, confirmedBy: "本人", confirmedAt: "09.12 09:10", permissions: [{ action, scope, mode: "已授权" | "可自动准备" | "先预览" | "每次确认" | "未授权", note }] }`。
- `ruleChangeLog[]`：`{ version, at, actor: "本人" | "AI 建议", diff }`——AI 建议只有本人确认后才改版本。
- `automation`：`{ paused: boolean, preservedState: "原始事项与未完成责任仍然可见" }`。

**交互**：切换规则分组；修改任一权限生成新版本与变更记录；“查看规则变更记录”展示版本历史；“立即暂停”一步停用全部自动化且保留事项可见性。

**状态**：
- 保护状态机入口：本地规则有效 / 外部写入中 / 外部待核验 / 已覆盖指定渠道（§05）。
- 权限申请：模型请求新权限时只能生成建议，等待本人逐条确认。
- 暂停态：暂停后不清空上下文，恢复时延续原状态。

**验收标准**：
1. 六项权限各自显示分级徽标与限制说明，无一仅靠颜色区分。
2. 规则版本（08）与确认时间（09.12 09:10）可见。
3. “立即暂停”一步可达，且保留事项可见性的说明存在。
4. “不得从其他许可推导授权”文案出现在付款行。

**必须保留的边界**：模型不能自行扩大权限；发送不视为可完全撤回；付款、购买与新增承诺不开放。

### s11 私人上下文 · 来源与推测

**设计大图**：`design/01_桌面大图/11_私人上下文_来源与推测.png`（对应图号 11）。

**目的**：把本人意图、事实来源与系统推测分层保存与呈现，提供修正、撤销与导出能力，让“了解你”不变成“占有你”。

**布局区域**：
- 标题区：“了解你，不等于占有你。”；副文案“事实、意图与系统推测分开保存。你可以修正，也可以带走。”
- 左卡页签：我的意图（选中）/ 事实与来源 / 系统推测。
- 意图区：“最近想留给自己的时间”；说明“由你明确表达 · 可修改、暂停或删除”；意图卡“☾ 工作日晚上，保留一段不被工作填满的时间。——最近想重新做音乐，但不要变成新的考核。来源：本人确认 · 09.11 20:10”。
- 推测区“01 一个尚未确认的推测”：“你可能更愿意从修改草稿开始。”；依据行“最近两次写作中的选择。样本很少，不代表稳定偏好，更不代表人格判断。”；pill“系统推测 · 7 天后重新检查”（暖色）；动作“这对我大致成立 / 并不准确 / 删除这条推测”。
- 右列“02 已选择的来源”四行：工作日历（只读 · 17:00 已同步）、任务清单（读取与内部规划 · 17:00 已同步）、两份指定文件（仅本次任务可用）、其他生活安排（未接入 · 不能推断没有安排，暖色）。
- 右列收束卡：“随时收回未来的访问。撤销权限会停止后续读取；已经传出的副本不能被假定全部收回。”；动作“管理来源权限”（盾形图标）与“导出我的记录”（上传图标）；页脚“不把点击、接受率或使用时长当作你的生活目标。”

**数据模型**：
- `intent`：`{ text, detail, provenance: "本人确认", confirmedAt, lifecycle: "可修改、暂停或删除" }`。
- `hypothesis`：`{ text, evidence, sampleNote, status: "本人表达" | "系统假设" | "被否定" | "过期待复查", recheckAfterDays: 7 }`；被否定后不再隐形套用（§05）。
- `sourceRegistry[]`：`{ name, access, lastSyncAt, scope }`；`其他生活安排` 的 \'未接入\' 不得参与任何“没有安排”推断。

**交互**：页签切换三层内容；意图可编辑、暂停、删除；推测可确认、否定或删除；确认后推测转为“本人表达”，仍可再修正；“管理来源权限”进入 s10；“导出我的记录”生成用户可带走的导出件。

**状态**：
- 推测状态机入口：本人表达 / 系统假设 / 被否定 / 过期待复查；过期推测重新征求确认，不自动续期。
- 空态：某层无内容时显示该层说明与录入入口。
- 边界态：撤销访问只停止后续读取，页面必须保持“已传出副本无法假定收回”的表述。

**验收标准**：
1. 意图行显示“由你明确表达 · 可修改、暂停或删除”。
2. 推测带“系统推测 · 7 天后重新检查”pill 与样本量说明。
3. “其他生活安排”显示“未接入 · 不能推断没有安排”。
4. 撤销语义与导出能力同屏可见。

**必须保留的边界**：推测不能升级为事实；了解你不等于占有你；不把行为指标当作生活目标；撤销只控制后续访问。

### s12 时间回顾 · 收益不做假账

**设计大图**：`design/01_桌面大图/12_时间回顾_收益不做假账.png`（对应图号 12）。

**目的**：把保住的时段、延期负担与 AI 管理成本分别记账，呈现真实失败，不伪造反事实净节省或人生评分。

**布局区域**：
- 标题区：“记下保留的生活，不夸大节省。”；副文案“本周回顾 · 9 月 7 日—11 日 · 以下均为合成演示数据。”
- 左卡：衬线数字“4 / 5”＋pill“✓ 由你回顾确认”；主句“段想保留的时间，得到了保留。”；“希望保留 300 分钟 · 实际保留 240 分钟”；周条形图：周一 60、周二 60、周三 0、周四 60、周五 60（周三为空条）；收束句“周三没有保住，不必把它藏起来。这不是连胜记录，也不是对生活的评分。”
- 右列“01 三种时间，分别记账”三行：
  1. 240 分钟（绿色衬线）——目标时段得到保留——“是观察结果，不等于产品净节省。”
  2. 40 分钟（暖色衬线）——延期到下一日的负担——“责任仍在，不能冒充已完成。”
  3. 12 分钟（蓝色衬线）——检查与管理 AI 的投入——“作为成本记录，而不是忽略。”
- 右列“02 真正的净节省，尚未测量”：“需要比较没有留白时的同类任务，并把监督、纠错与未来负担计入。当前不显示一个看似精确的‘省时总数’。”
- 反馈区：“这一周，你对自己的时间是否更有掌控？”；选项“更有掌控 / 差不多 / 反而更难 / 跳过”。

**数据模型**：
- `weeklyReview`：`{ weekRange, targetMinutes: 300, keptMinutes: 240, perDay: [{ day, kept }], deferredBurdenMinutes: 40, aiManagementMinutes: 12, netSavings: "未测量", confirmedByUser }`。
- `feedback`：`{ question, options: ["更有掌控", "差不多", "反而更难"], skipped: boolean }`——低频、可跳过，不计入任何评分体系。

**交互**：回顾数据需本人确认后定稿；反馈单选或跳过；点击任一记账行查看构成明细；页面不提供任何“省时总数”汇总入口。

**状态**：
- 空态：本周无保留记录时显示“没有可回顾的保留时段”，不显示 0 分或评分。
- 部分失败：某天未保住时如实显示空条（周三 0）。
- 边界态：延期负担未清偿前持续保留在账面，不得标记“已完成”。

**验收标准**：
1. 三种时间分列三行，颜色与注释各自独立，无合计行。
2. “真正的净节省，尚未测量”与“不显示省时总数”文案可见。
3. 周三 0 分钟如实显示，且页面无连胜/评分元素。
4. 反馈四选项含“跳过”。

**必须保留的边界**：不能伪造净节省与人生评分；保留时段是观察结果，不等于产品净节省；延期负担不能冒充已完成。

### s13 同步异常 · 未知与接管

**设计大图**：`design/01_桌面大图/13_同步异常_未知与接管.png`（对应图号 13）。

**目的**：在部分同步失败时把本地保护与外部保护分开呈现：本地规则仍然有效，外部状态保持未知，并给出重试、原工具接管与降级三条出路。

**布局区域**：
- 标题区：“不知道的时候，就说不知道。”；副文案“数据未同步不等于没有安排。外部保护未核验，就保持待确认。”
- 左卡：pill“⚠ 部分来源未同步”；主标题“今晚的保护，尚未完整核验。”；说明“留白中的规则仍然保留。但因为个人日历暂时无法更新，不能确认外部预约是否看见这段不可用时间。”；绿色确认卡“✓ 本地规则仍然有效——19:00—20:00 不接受留白内部的自动填充。”；暖色待确认卡“⚠ 外部日历保护待确认——尚未核验忙闲投影与最新冲突。不会显示‘已全面保护’。”；动作“重新同步与核验”（主动作，刷新图标）与“打开原日历 ↗”；脚注“失败记录会保留；重试不会重复创建同一保护事件。”
- 右列“01 目前知道什么”：工作日历 17:02 已同步；任务清单 17:00 已同步；个人日历 16:41 后未同步（暖色）；家庭日历 尚未连接。
- 右列收束卡：“可以继续，不必假装完整。暂时使用本地计划时，不会把未知时段分配给新的自动任务；你仍可以在原工具中接管。”；动作“暂用本地计划 →”。

**数据模型**：
- `syncStatus[]`：`{ source, lastSuccessAt, state: "已同步" | "过期" | "同步失败" | "尚未连接" }`。
- `protectionState`：`{ local: "有效", external: "待核验" | "写入中" | "已覆盖指定渠道" }`（§05 保护状态机）。
- `retry`：`{ idempotent: true, failureLog: [] }`；重试按保护事件幂等键去重。

**交互**：“重新同步与核验”按幂等键重试并在完成后更新外部状态；“打开原日历”跳出原工具接管；“暂用本地计划”进入降级模式——未知时段不参与自动分配。

**状态**：
- 保护状态机入口：本地规则有效 / 外部写入中 / 外部待核验 / 已覆盖指定渠道。
- 未知态：外部忙闲未核验时显示“待确认”，任何页面不得展示“已全面保护”。
- 失败态：保留上一份可用数据并标注时间；失败记录保留供排查。

**验收标准**：
1. 本地“仍然有效”与外部“待确认”两张状态卡并存且可区分。
2. “不会显示‘已全面保护’”文案可见。
3. 四个来源各显示最后成功时间或连接状态。
4. “重试不会重复创建同一保护事件”与“不会把未知时段分配给新的自动任务”可见。

**必须保留的边界**：外部保护未核验就保持未知；数据未同步不等于没有安排；可以降级继续，不必假装完整。

### s14 快捷输入 · 意图与约束

**设计大图**：`design/01_桌面大图/14_快捷输入_意图与约束.png`（对应图号 14）。

**目的**：把一句话输入拆成“意图”与“硬约束”，先理解再检查冲突，不从口语输入推导任何泛化授权。

**布局区域**：
- 标题区：“一句话，保留你的原意。”；副文案“轻量输入不意味着草率执行。先理解，再检查可行性。”
- 快捷入口卡（ESC 关闭）：原话衬线展示“明晚七点到八点留给自己，客户会议别动。”＋语音输入图标。
- “系统理解为”两张并列卡：
  1. ☾ 个人时间意图——9 月 13 日 · 19:00—20:00——当前时区 UTC+8 · 可修改 · 尚未创建日历事件。
  2. 🔒 不可擅自改变——现有客户会议——没有自动授予改会或发消息权限。
- 说明行：“下一步只检查冲突。需要改变安排时，再向你展示具体差异。”
- 动作：检查这段时间（主动作，放大镜图标）/ 只保存为意图；快捷键提示“⌘ Enter”；卡底“快速记录 / 自然语言 / 语音 / 分享到留白 · 同一套语义与授权边界”。

**数据模型**：
- `capture`：`{ verbatim, channel: "快速记录" | "自然语言" | "语音" | "分享", parsed: { intent: { type: "个人时间意图", start, end, timezone, calendarEventCreated: false }, constraints: [{ object: "现有客户会议", rule: "不可擅自改变", granted: [] }] } }`。
- `conflictCheck`：`{ status: "未检查" | "无冲突" | "有冲突", diffs? }`——有冲突时进入差异展示，不直接执行。

**交互**：解析结果可逐字段修改；“检查这段时间”运行冲突检查并按结果展示差异或确认保存；“只保存为意图”仅存意图，不创建日历事件；ESC 关闭不丢失已保存内容之外的输入草稿。

**状态**：
- 解析不确定：展示理解结果并标注可修改，不静默采用。
- 冲突态：与保护时段或客户会议冲突时只展示差异，等待用户决定。
- 空态：输入为空时显示来源、示例与手动记录入口（§05）。

**验收标准**：
1. 意图卡显示“尚未创建日历事件”。
2. 约束卡显示“没有自动授予改会或发消息权限”。
3. 两个动作均不产生任何外部变更。
4. 底部声明四个入口共用同一套语义与授权边界。

**必须保留的边界**：输入一句话不等于泛化授权；下一步只检查冲突，改变安排前展示具体差异；改会、发消息权限不被自动授予。

### s15 移动端 · 此刻

**设计大图**：`design/02_移动端大图/15_移动端_此刻.png`（对应图号 15，1440×3120 @3×）。

**目的**：手机首屏只保留“一段时间”与“一个必要决定”，关键控制用明确文字而非仅图标，不显示任务完成量作为中心。

**布局区域**：
- 状态栏（17:00）与品牌行：留白＋“⋯”菜单；日期行“9 月 12 日 · 星期六”。
- 叙事标题：“把今晚，留给自己。”＋右上角椭环空间视觉。
- 时间意图卡：锁形图标＋“你的时间意图”；衬线大字 19:00—20:00；“不自动填满。没有用途，也成立。”；暖色警告条“⚠ 当前计划超出容量 10 分钟”（整宽 pill）。
- 决定区标题“现在，只需一个决定。”；决策卡“✦ 换一种做法，保住这一小时。”；“AI 先准备报告，你从检查开始。预计减少投入，不悄悄降低交付标准。”；主动作“查看两条路径 →”（全宽）。
- 约束脚注行：“会议不动 · 期限不变 · 没有执行任何变更”；演示声明“界面演示 · 非真实账户数据”。
- 底部标签栏：此刻 / 时间 / 协同 / 我的（四项，文字＋图标，此刻选中）。

**数据模型**：与 s01 同构（`protectedBlock`、`capacity`、`decision`），另加 `mobileTab`；决策卡在本屏压缩为单条主推路径，两条路径的完整比较在 s16。

**交互**：时间卡点击进入 s02 时间账本；主动作进入 s16；标签栏切换四个分区；⋯ 菜单承载设置与边界入口。

**状态**：
- 空态：无保护时段时以同尺寸卡片提供“设置一段保护时间”入口。
- 失败/过期：同步异常时警告条替换为“数据未同步”说明并保留上次数据（§05）。
- 边界态：不出现完成量、连续记录或任何进度指标。

**验收标准**：
1. 首屏主对象为 19:00—20:00 保护时段与单一决定。
2. 容量缺口 10 分钟以整宽文字条呈现。
3. 主动作与标签栏均为带文字的控制，触控目标 ≥44 CSS 像素（§07）。
4. 页面无任务完成量元素；“没有执行任何变更”可见。

**必须保留的边界**：不显示任务完成量作为中心；不自动填满；没有用途也成立。

### s16 移动端 · 自适应方案

**设计大图**：`design/02_移动端大图/16_移动端_自适应方案.png`（对应图号 16）。

**目的**：在单列移动版式上保留估计、管理成本与未来负担的完整信息，固定底部动作只进入授权预览而不直接执行。

**布局区域**：
- 品牌行“可行方案”＋“⋯”；叙事标题“不挤掉自己，换一种做法。”
- 容量摘要条：“120 分钟可用 / 130 分钟预计投入”。
- 方案 A 卡（展开态）：pill“方案 A · 先准备，再判断”；衬线数字 60 → 40 分钟；“报告的预计人工投入，包含你检查和修改草稿的时间。”；明细行：预计减少＝20 分钟（绿色）、外部承诺＝不变；脚注“下一步仅准备草稿，仍需你检查。”
- 方案 B 卡（收起态）：“方案 B · 行政事项移到明天——延期 40 分钟，不算净节省。”（右侧展开箭头）。
- 固定底部主动作：“预览方案 A 的授权 →”；演示声明；底部标签栏（协同选中）。

**数据模型**：与 s04 同构（`plan`、约束 chips 简化为摘要条）；`selectedPlan: A`；`bottomAction: { type: "预览授权", targetPlanVersion }`——不存在“直接执行”动作。

**交互**：方案 B 行展开显示延期明细（明天新增负担 40 分钟、净节省没有发生）；展开 A/B 后底部按钮跟随所选方案；底部按钮仅进入 s17 授权预览。

**状态**：
- 空态：无候选方案时显示容量摘要与来源说明。
- 失效态：来源变化后显示“方案需要重新检查”，底部按钮禁用并给出理由（§05：停用按钮必须说明原因）。
- 边界态：估计变化后返回容量检查（§06）。

**验收标准**：
1. “预计减少 20 分钟”与“延期 40 分钟，不算净节省”两个数字限定语完整。
2. 方案 B 收起态仍显示“不算净节省”。
3. 固定底部按钮文案为“预览……授权”，不存在执行类按钮。
4. 固定按钮不覆盖正文，字体放大后可纵向滚动（§07）。

**必须保留的边界**：固定底部动作不能跳过批准；估计、净减少与未来负担分开。

### s17 移动端 · 一次性授权

**设计大图**：`design/02_移动端大图/17_移动端_一次性授权.png`（对应图号 17）。

**目的**：在小屏上完整呈现允许的三项动作与排除项，批准之前所有动作仍未执行。

**布局区域**：
- 品牌行“变更预览”＋“⋯”；叙事标题“只授权这一次。”；元信息行“方案版本 03 · 尚未执行”。
- 授权卡：pill“🛡 允许的范围”；三项勾选（深色勾选框）：
  1. 读取两份指定材料——“不扩展到全部文件与邮件”。
  2. 创建待检查的草稿——“不代表已完成或已发送”。
  3. 更新内部投入估计——“暂估 40 分钟，允许修正”。
- 分隔线后锁定行：“🔒 不包含：移动会议、发消息、付款或新增承诺。”
- 固定底部主动作“批准并准备草稿 →”；次动作链接“返回修改方案”；演示声明；底部标签栏（我的选中）。

**数据模型**：与 s05 同构（`changeSet`、`approval`）；`grants` 三项与 `exclusions` 一行汇总（移动会议、发消息、付款、新增承诺）；`status: "尚未执行"`。

**交互**：三项授权不可在小屏折叠隐藏；批准后进入草稿生成（状态待检查）；“返回修改方案”回到 s16 且不丢失候选方案；任何参数变化使批准失效。

**状态**：
- 待批准为默认态；“尚未执行”元信息可见。
- 失效态：版本变化后显示旧批准不可用并要求重新批准（§05）。
- 不可逆边界：排除项保证本屏不存在不可逆外部授权；已发出的内容不因退出界面而撤销。

**验收标准**：
1. 三项授权与排除项同屏可见，无需滚动展开隐藏面板。
2. “方案版本 03 · 尚未执行”可见。
3. 批准动作仅授权本次，页面无常驻授权入口。
4. 排除行覆盖发送、改会与付款。

**必须保留的边界**：不包含发送、改会与付款；排除项不可因小屏隐藏；固定底部动作不能跳过本预览。

### s18 移动端 · 留白时刻

**设计大图**：`design/02_移动端大图/18_移动端_留白时刻.png`（对应图号 18）。

**目的**：提供最简洁的个人时间状态：一个主动作、无压力元素、边界覆盖明确，什么也不安排即成立。

**布局区域**：
- 状态栏（19:00）与品牌行：留白＋右上角退出按钮（×）。
- 居中区域：字距拉宽的英文行“A SPACE OF YOUR OWN”；大面积开放椭环视觉；衬线标题“这段时间，不必证明什么。”；时间“19:00—20:00”；“不自动填入工作，也不催促你选择。”
- 唯一主动作（全宽深色按钮）：“什么也不安排”。
- 页脚：“只保护已接入渠道 · 设计演示”。

**数据模型**：与 s08 同构（`quietSession`）；移动端将选择压缩为单一主动作，音乐/探索等入口留在桌面完整版；“不催促”标记 `rePromptAfterNoChoice: false`。

**交互**：“什么也不安排”关闭安静界面且不再催促；“×”随时退出；无其他动作要求。

**状态**：
- 安静态：无倒计时、无打卡、无绩效指标；动效遵循系统减少动态效果（§07）。
- 边界态：页脚声明“只保护已接入渠道”，不夸大保护范围。
- 退出后：状态保留在 s01 的时间意图卡，不产生遗留任务。

**验收标准**：
1. 全页仅一个主动作按钮“什么也不安排”。
2. 无倒计时、无连续记录、无完成量、无催促文案。
3. 退出按钮（×）可见可达。
4. “只保护已接入渠道”边界声明可见。

**必须保留的边界**：无绩效、打卡与倒计时压力；什么也不安排即成立；保护范围如实标注。

## 5. 主要流程

### 5.1 保住一段时间

用户表达意图 → 获取经授权的当前事项 → 检查来源覆盖与容量 → 生成不同方法 → 用户选择 → 展示具体变更 → 检查有效授权 → 准备或执行 → 回读结果 → 更新责任与时间账本 → 进入个人时间。

涉及屏幕：s01（意图与缺口）→ s02（容量与来源）→ s04（路径选择）→ s05（授权预览）→ s06（草稿）→ s02/s12（账本与回顾）→ s08/s18（个人时间）。

**规则**：不把“生成计划”“批准计划”“执行成功”“外部结果核验”“本人感到受益”折叠成同一个完成状态；每一步必须显示自身状态词（§6）。

### 5.2 工作被打断后继续

保存检查点 → 重新打开时核对来源版本 → 恢复已确认部分 → 显示一个未决判断 → 用户选择继续、口述、补材料或暂不处理。

涉及屏幕：s07（检查点恢复）→ s06（继续核对）→ s14（口述/快捷输入）。

**检查点必须包含**：产物版本、上次确认的取舍、尚未解决的问题、关联材料与下一步。模型自己的推测不能占据“已确认”位置；恢复界面不得发明进度。

### 5.3 外部请求形成责任

原始请求 → 候选事项 → 用户确认是否接受 → 明确责任范围 → 再讨论截止时间和投入。

涉及屏幕：s14/s03（进入收件箱）→ s03（确认或保留为想法）→ s01/s02（责任入账）。

**规则**：一项请求可以保留为想法，也可以被排除；接受新责任的动作，与对外发送“我来做”的消息必须区分——后者永远属于“每次确认”权限（s10）。

## 6. 状态机

六个对象的状态词即责任边界，不只是视觉标签（§05）：

| 对象 | 状态 | 展示原则 |
|---|---|---|
| 方案 | 候选 / 选中 / 待批准 / 失效 | 未批准不写成已安排；来源变化后提示重新检查 |
| 操作 | 未开始 / 执行中 / 核验中 / 已核验 / 失败 / 未知 | 执行中说明当前动作，不虚构百分比进度 |
| 草稿 | 生成中 / 待检查 / 部分确认 / 已确认 / 已发送 | 状态词是责任边界；未核对内容保持可见 |
| 保护 | 本地规则有效 / 外部写入中 / 外部待核验 / 已覆盖指定渠道 | 不把局部覆盖宣传为全系统保护 |
| 来源 | 已同步 / 过期 / 同步失败 / 未连接 | 显示最后成功更新时间和覆盖范围 |
| 个性化 | 本人表达 / 系统假设 / 被否定 / 过期待复查 | 被否定后不继续隐形套用旧假设 |

**反馈细节**：
- 正常加载使用轻量骨架或状态文字，不做无限旋转的“AI 正在理解你”。
- 失败保留上一份可用数据并标注过期，不回退为空列表。
- 输入为空时显示来源、示例与手动记录入口，不伪造现成任务。
- 悬停显示轻微描边与操作提示；按下减少阴影并做极小位移；键盘焦点使用可见蓝灰外框（`--focus`）。
- 危险操作不只靠颜色区分，必须保留具体文字；停用按钮同时给出不可操作的理由，不能只有低透明度。
- 确认框的关闭和返回不丢失候选方案；若已发生不可逆外部动作，关闭界面不能被解释为撤销；撤销访问只控制后续访问，不承诺收回所有外部副本。

## 7. 时间账本算术规范

以下算术（§06）为设计情境与记账规范，不是实际任务耗时或研究效果数据。

**基准情境**：17:00—19:00 的可用容量为 120 分钟。会议 30、报告预计 60、行政事项 40，合计 130 分钟，存在 **10 分钟缺口**。

**方案 A（改变方法）**：报告先由 AI 准备，预计人工投入 40 分钟，包含用户检查与修改。合计 30 + 40 + 40 = **110 分钟**。“预计减少 20 分钟”是估计，**并非已测量的事实**；实际核对超过估计时必须重新检查容量。页面呈现：预计减少、19:00 前预计总投入（110/120）、仍需本人完成的内容，三行分开。

**方案 B（延期）**：行政事项移至未来，当前合计 30 + 60 = **90 分钟**，但未来仍欠 40 分钟。**不得显示“节省了 40 分钟”**；预计净节省记为“没有发生”，40 分钟记为“明天新增负担”。

**保住时段的记账**：保住 19:00—20:00 这一小时，**不能宣称“AI 节省了一小时”**。保留目标时段与减少人工投入分别记录（s12 三行账目），避免重复记功；净节省在缺少对照与成本核算前一律记为“尚未测量”。

**实现要求**：账本任何汇总视图不得出现跨类合计（保留 + 减少 + 延期）或“省时总数”；延期负担未清偿前持续留在账面。

## 8. 移动端与无障碍

**移动端行为**（§07）：
- 主流程单列，关键动作靠近底部；固定底部按钮不得覆盖正文，字体放大后允许纵向滚动。
- **授权的排除项不能因小屏幕而被隐藏**（s17 三项授权与排除行同屏）。
- 安静界面保留清晰退出入口，不困住用户（s18 的 ×）。

**无障碍实施要求**：
- 所有交互需要语义标签、可见键盘焦点与屏幕阅读顺序。
- 自动排程不能只依赖拖拽；每项时间变更还应提供表单或键盘路径。
- 触控目标按至少 **44 CSS 像素**设计。
- 图标与颜色不作为唯一的状态表达；状态永远伴随文字。

**动效（后续实施规范）**：面板入场 160—220 毫秒，确认状态 120—180 毫秒；安静界面的装饰不持续抢注意力；跟随系统“减少动态效果”设置。当前 PNG 不包含真实动效或交互实现。

## 9. 验证策略与验收边界

**证据结构（RED → GREEN）**：每个验收标准先以失败测试落档于 `evidence/red/sNN.txt`（stub 屏缺少目标文案/元素时失败），实现转绿后对应证据归档于 `evidence/green/`，界面截图存于 `evidence/screenshots/`。没有对应 GREEN 证据的标准不得标记为已满足；只有 RED 记录时，该屏状态为“已定义、未实现”。

**验收标准分类（C1–C5）**：第 4 节每屏的验收标准按以下五类组织——

| 类别 | 含义 | 典型断言 |
|---|---|---|
| C1 内容与边界文案 | 关键诚实文案与边界声明可见 | “还没有修改任何安排”“不算净节省”“只保护已接入渠道”渲染可见 |
| C2 布局与令牌 | 区域结构与 §02 令牌一致 | 双轨/单列结构、唯一主动作、暖色缺口 pill、衬线时间数字 |
| C3 交互与流程 | 动作与状态转换按第 4—5 节工作 | 方案→预览→批准链路；确认不触发发送；退出保留候选 |
| C4 状态行为 | 空态/失败/未知符合 §05 与第 6 节 | 失败保留旧数据并标注过期；未知保持未知；停用给理由 |
| C5 边界保持 | “必须保留的边界”不被任何实现路径违反 | 请求不自动升级承诺；推测不升级事实；无倒计时/打卡/完成量；收益不做假账 |

**测试组织**：`tests/groupA.spec.ts`（s01–s05，桌面）、`tests/groupB.spec.ts`（s06–s10，桌面）、`tests/groupC.spec.ts`（s11–s14，桌面）与移动端规格；每条用例定位 `[data-page="sNN"]` 断言 C1–C5 子集。

**验收边界（§08，原样保留）**：本包完成了 18 个独立页面的图片尺寸检查、文字出界检查与文字重叠检查，并做过渲染目视审阅。它**不是**可访问性认证、真实账户端到端测试或用户效果试验。页面索引只浏览图片。正式接入之前，仍需实现来源同步、权限验证、并发变更检查、幂等执行、结果回读与真实撤销语义。

## 10. 非目标

本包（V2 演示态）明确不包含：

- **真实集成**：不连接真实日历、邮件、任务系统或任何外部账户；所有“已同步”数据均为合成演示数据。
- **真实收益声明**：不产生、不暗示任何真实节省、效率提升或用户效果结论；所有算术为 §07 设计情境。
- **纯演示数据之外的持久化承诺**：演示数据不代表已连接、已执行或真实收益（每屏页脚声明）。
- **可访问性认证与用户试验**：不替代 WCAG 审计、真实账户端到端测试或用户研究（§08、§9）。
- **真实动效实现**：PNG 不包含动效；动效参数仅为后续实施规范（§07）。
- **全系统通知接管**：注意力预算只覆盖已接入请求，不做操作系统级通知接管（s09）。
